version: "2.4"
name: my-project

services:
  web:
    image: nginx:latest
    command: nginx -g 'daemon off;'
    ports:
      - 80:80
    networks:
      - my-network
    volumes:
      - my-volume:/var/www/html

networks:
  my-network:

volumes:
  my-volume: