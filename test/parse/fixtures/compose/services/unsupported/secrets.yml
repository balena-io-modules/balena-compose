services:
  main:
    image: alpine:latest
    command: sh -c "sleep infinity"
    secrets:
      - my_secret
      - my_other_secret

secrets:
  my_secret:
    external: true
  my_other_secret:
    external: true