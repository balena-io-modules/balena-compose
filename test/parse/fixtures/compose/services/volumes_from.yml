services:
  one:
    image: alpine:latest
    command: sh -c "sleep infinity"
    volumes_from:
      - two:ro
  two:
    image: alpine:latest
    command: sh -c "sleep infinity"
    volumes:
      - test:/test

volumes:
  test: